name: Deploy Frontend

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    if: contains(github.event.head_commit.message, '[deploy]')
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy to VPS
        uses: appleboy/ssh-action@master
        with:
          host: 72.61.255.252
          username: root
          key: ${{ secrets.SSH_KEY }}
          script: |
            cd /var/www/frontend
            git pull origin main
            npm install
            npm run build
            pm2 restart wewon-frontend